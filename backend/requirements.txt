# ─────────────────────────────────────────────
#  SIMS Backend — Python 3.11 Dependencies
#  Phase 1: Core scaffold + all future phases
# ─────────────────────────────────────────────

# ── Web Framework ────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
httpx==0.27.0                  # async HTTP client (VirusTotal)
aiohttp==3.9.5                 # async HTTP (alternative)

# ── Data Validation ──────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1       # BaseSettings for .env loading

# ── Machine Learning (inference only — training uses separate env) ──
numpy==1.26.4
scikit-learn==1.4.2            # metrics helpers

# ── NLP / Tokenization ───────────────────────
transformers==4.40.1           # HuggingFace BERT tokenizers
tokenizers==0.19.1             # Fast tokenizer backend
datasets==2.19.1               # HuggingFace datasets loader

# ── Data Engineering ─────────────────────────
pandas==2.2.2
openpyxl==3.1.2                # Excel file reading
pyarrow==16.0.0                # Parquet support for HF datasets

# ── Database ─────────────────────────────────
supabase==2.4.6                # Supabase Python client (PostgreSQL)

# ── Security / Auth ──────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── Monitoring / Logging ─────────────────────
loguru==0.7.2                  # structured logging
python-dotenv==1.0.1           # .env file loading

# ── Testing ──────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
httpx==0.27.0                  # TestClient for FastAPI

# ── Dev Utilities ────────────────────────────
requests==2.31.0               # sync HTTP (scripts)
tqdm==4.66.4                   # progress bars
rich==13.7.1                   # rich terminal output
kaggle==1.6.12                 # Kaggle dataset downloader
